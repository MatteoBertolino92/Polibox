Polibox is a simple client-server system which allows the user to monitor a folder.
Each modification in the folder is sent to the server which keep an history of the last versions.
It is possible recovering the file modified/deleted by error and selecting the desider version.
SSLStream based. Realized for the academic course System and Device Programming, Polito.